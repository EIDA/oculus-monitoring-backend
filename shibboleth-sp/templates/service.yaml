apiVersion: v1
kind: Service
metadata:
  name: {{ include "shibboleth-sp.fullname" . }}
  labels:
    {{- include "shibboleth-sp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shibboleth-sp.selectorLabels" . | nindent 4 }}
