apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shibboleth-sp.fullname" . }}-test-connection"
  labels:
    {{- include "shibboleth-sp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shibboleth-sp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
